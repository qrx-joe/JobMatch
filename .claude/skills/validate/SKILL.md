---
description: Build validation for JobMatch frontend
---

Run build validation for the JobMatch frontend:

1. Change to frontend directory: `cd D:/code/JobMatch/webapp/frontend`
2. Run: `npm run build`
3. If build fails, report all errors and stop
4. If build succeeds, report "Build OK" with chunk sizes

Scope: Only operate on `webapp/frontend`. Do not modify code during validation.
